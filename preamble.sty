\RequirePackage{iflang}%         % language checks
\RequirePackage{mathtools}      % Mathebefehle, lädt intern amsmath
\RequirePackage{amsthm}%         % Matheumgebungen wie theorem oder proof
\RequirePackage{amssymb}        % Weitere Mathe-Symbole

\RequirePackage[autostyle]{csquotes} % correct quotation marks

\RequirePackage{graphicx}       % Einbindung von Bildern
\RequirePackage{microtype}      % improved micro-typography: character protrusion and font expansion are enabled by default.

\RequirePackage{bbm}            % Indikator-Funktion
\RequirePackage{cancel}         % Durchstreichen von Termen

\RequirePackage{booktabs}       % Bessere Tabellen
\RequirePackage{tabularx}       % Tabellen mit Zusatzfunktionen

\RequirePackage{algpseudocode}  % Pseudocode
\algrenewcommand\algorithmicrequire{\textbf{Input}}
\algrenewcommand\algorithmicensure{\textbf{Output}}


\RequirePackage[%               % Euro-Zeichen setzen
official%                       %     %Felix: Argument war Teil des Ursprungspakets, mir ist unklar, was es tut
]{eurosym}                      %

\RequirePackage{soul}           % Unter- und Durchstreichen von Text-Passagen

\RequirePackage[%               % colors
    dvipsnames%                 %     gives more colors names
]{xcolor}

\RequirePackage[hidelinks,breaklinks]{hyperref}

% Uni Bonn Farben
\definecolor{ub-blue}{cmyk}{1,0.7,0,0}
\definecolor{ub-yellow}{cmyk}{0,0.3,1,0}
\definecolor{ub-gray}{cmyk}{0,0,0.15,0.55}

\hypersetup{colorlinks=true,
            linkcolor=black,
            filecolor=black,
            urlcolor=ub-blue,
            citecolor=ub-blue,
            bookmarksnumbered=true
            }



\renewcommand*\url[1]{\href{https://#1}{\nolinkurl{#1}}}
% The original command does not work when #1 does not begin with "www".

\newcommand*\mail[1]{\href{mailto:#1}{\nolinkurl{#1}}}

\renewenvironment{proof}[1][\proofname]{\par
\pushQED{\qed}%
\normalfont \topsep 0pt\relax% former: \topsep6\p@\@plus6\p@\relax
\trivlist
\item\relax
{\itshape
#1\@addpunct{.}}\hspace\labelsep\ignorespaces
}{%
\popQED\endtrivlist\@endpefalse
}

%%
%% Further Packages that might be useful
%%

%\RequirePackage{gauss}        % Hilfreich zur Darstellung des Gauss-Algo
%\RequirePackage{tikz}

%%
%% TikZ (zum Zeichnen)
%%

%\usetikzlibrary{arrows,%
%                petri,%
%                topaths,%
%                arrows.meta,%
%                positioning,%
%                automata}%
%\RequirePackage{tkz-berge}
